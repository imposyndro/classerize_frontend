# AI Usage Tracker (Prisma) — local SQLite dev database.
# Copy this file to `.env` before running `npx prisma migrate deploy` / `npm run seed`.
# For production, point at Postgres and set datasource `provider = "postgresql"`
# in prisma/schema.prisma.
DATABASE_URL="file:./dev.db"
