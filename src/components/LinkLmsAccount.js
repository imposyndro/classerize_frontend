"use client";

// This component has been superseded by CanvasLinker.js (which uses the correct
// cookie-based auth flow). The localStorage JWT approach used here was incorrect.
// Left as an empty export to avoid breaking any import that still references it.
// Fully replaced in Phase 3 with a tabbed multi-LMS linker in Settings.
export default function LinkLmsAccount() {
    return null;
}
